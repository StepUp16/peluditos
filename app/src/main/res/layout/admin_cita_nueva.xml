<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondos_celular">

    <!-- Botón de retroceso -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/btn_back_desc"
        android:src="@drawable/ic_arrow_back"
        app:tint="@color/textColorPrimary"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- ScrollView para los campos -->
    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/btnBack"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnCrearCita">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:paddingTop="64dp"
            android:paddingBottom="96dp">

            <!-- Título -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/title_nueva_cita"
                android:textColor="@color/textColorPrimary"
                android:textStyle="bold"
                android:textSize="22sp"
                android:gravity="center"
                android:layout_marginBottom="24dp"/>

            <Spinner
                android:id="@+id/spinnerCliente"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/edittext_bg"
                android:padding="16dp"
                android:layout_marginBottom="16dp"
                android:spinnerMode="dropdown"
                android:prompt="@string/hint_cliente"/>

            <Spinner
                android:id="@+id/spinnerPaciente"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:background="@drawable/edittext_bg"
                android:padding="16dp"
                android:prompt="@string/hint_paciente"
                android:spinnerMode="dropdown" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/label_estado_cita"
                android:textStyle="bold"
                android:textSize="16sp"
                android:layout_marginBottom="8dp"/>

            <Spinner
                android:id="@+id/spinnerEstado"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/edittext_bg"
                android:padding="16dp"
                android:layout_marginBottom="16dp"
                android:spinnerMode="dropdown"
                android:prompt="@string/placeholder_select_estado"/>

            <!-- Campo Fecha con icono de calendario -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:orientation="horizontal"
                android:background="@drawable/edittext_bg"
                android:layout_marginBottom="16dp"
                android:gravity="center_vertical">
                <EditText
                    android:id="@+id/etFecha"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@android:color/transparent"
                    android:hint="@string/hint_fecha"
                    android:paddingStart="16dp"
                    android:textColorHint="@color/textColorPrimary"
                    android:textSize="18sp"
                    android:inputType="date"/>
                <ImageView
                    android:id="@+id/iconFecha"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/icono_citas"
                    android:contentDescription="Calendario"
                    android:layout_marginEnd="16dp"
                    app:tint="@color/texto_oculto"/>
            </LinearLayout>

            <!-- Campo Hora con icono de reloj -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:orientation="horizontal"
                android:background="@drawable/edittext_bg"
                android:layout_marginBottom="16dp"
                android:gravity="center_vertical">
                <EditText
                    android:id="@+id/etHora"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@android:color/transparent"
                    android:hint="@string/hint_hora"
                    android:paddingStart="16dp"
                    android:textColorHint="@color/textColorPrimary"
                    android:textSize="18sp"
                    android:inputType="time"/>
                <ImageView
                    android:id="@+id/iconHora"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/icono_alarm"
                    android:contentDescription="Reloj"
                    android:layout_marginEnd="16dp"
                    app:tint="@color/texto_oculto"/>
            </LinearLayout>

            <!-- Campo Motivo -->
            <EditText
                android:id="@+id/etMotivo"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:background="@drawable/edittext_bg"
                android:hint="@string/hint_motivo"
                android:padding="16dp"
                android:textColorHint="@color/textColorPrimary"
                android:textSize="18sp"
                android:layout_marginBottom="16dp"
                android:inputType="text"/>

            <!-- Campo Notas Adicionales (multilinea) -->
            <EditText
                android:id="@+id/etNotasAdicionales"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:background="@drawable/edittext_bg"
                android:hint="@string/hint_notas_adicionales"
                android:padding="16dp"
                android:textColorHint="@color/textColorPrimary"
                android:textSize="18sp"
                android:inputType="textMultiLine"
                android:gravity="top|start"
                android:maxLines="5"
                android:layout_marginBottom="24dp"/>

        </LinearLayout>
    </ScrollView>

    <!-- Botón Guardar Cita fijo -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCrearCita"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:text="@string/btn_guardar_cita"
        android:fontFamily="@font/spline_sans_bold"
        android:minHeight="48dp"
        android:paddingVertical="12dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="8dp"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="16sp"
        app:backgroundTint="@color/mi_verde_primario"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toTopOf="@+id/bottomMenu"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomMenu"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        app:menu="@menu/menu_admin"

        app:itemIconTint="@color/selector_menu_admin"
        app:itemTextColor="@color/selector_menu_admin"

        app:labelVisibilityMode="labeled"

        android:background="@color/navigationBarBackground" />

</androidx.constraintlayout.widget.ConstraintLayout>
